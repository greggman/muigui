import Layout from './layout.js';
import Row from './row.js';

export default class Tabs extends Layout {
  constructor() {
    super('muigui-tabs');
    //const outer = new Row();
    //const tabArea = new Row()
    //const contentArea = new Row();
    //outer.add(tabArea);
    //contents.add(contentArea);
  }
}